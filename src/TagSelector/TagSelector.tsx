import suggestion from "./suggestion";

export const TagSelector = () => {
	return (
		<div id="tag-selector">
			<input
			// onKeyUp={suggestion.items.call(suggestion.render().onKeyDown.call(suggestion.render().onKeyDown)}
			></input>
		</div>
	);
};
